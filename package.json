{
  "name": "trigger-dynamo",
  "version": "1.0.0",
  "description": "",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke-local": "sls invoke local -f hero-insert --path mocks/heroes.insert.json",
    "invoke-local-trigger": "sls invoke local -f hero-trigger --path mocks/heroes.trigger.json",
    "invoke": "sls invoke -f hero-insert --path mocks/heroes.insert.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1085.0",
    "joi": "^17.6.0",
    "uuid": "^3.3.2"
  }
}
